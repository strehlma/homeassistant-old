cover:
  - platform: template
    covers:
      office:
        friendly_name: "Büro"
        position_template: "{{ state_attr('cover.office_hmip', 'current_position') }}"
        open_cover:
          service: cover.open_cover
          data:
            entity_id: cover.office_hmip
        close_cover:
          service: cover.close_cover
          data:
            entity_id: cover.office_hmip
        stop_cover:
          service: cover.stop_cover
          data:
            entity_id: cover.office_hmip
        set_cover_position:
          service: cover.set_cover_position
          data_template:
            entity_id: cover.office_hmip
            position: >
              {% set desired_position = "{{ position }}" %}
              {% set requested_position = ( (desired_positon * 0.8) | round 0 ) %}
              {{ desired_position }}
            
