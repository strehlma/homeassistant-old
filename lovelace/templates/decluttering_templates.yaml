# These are the decluttering templates. This file will
# only redirect to the corresponding templates. You will
# need this file or templates will not work!

# Header Text
header_text:
  !include header_template.yaml

# Button Card (This is the main template used everywhere)
switch_button_card:
  !include switch_template.yaml

# Person Picture Elements
person_elements:
  !include person_template.yaml

# Mini Graph Card
mini-graph:
  !include mini_graph_template.yaml

# Button Card (This is the main template used everywhere)
button:
  !include button_template.yaml

# Notification Card (Frontend)
notification: 
  !include notification_template.yaml
